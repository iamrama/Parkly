<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MultipleApiCallRoom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="Theme.MultipleApiCallRoom" parent="Base.Theme.MultipleApiCallRoom" />

    <!-- Add this Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color. -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- Set the icon to be displayed. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>

        <!-- Set the theme to be used after the splash screen is dismissed. -->
        <item name="postSplashScreenTheme">@style/Theme.MultipleApiCallRoom</item>
    </style>
</resources>